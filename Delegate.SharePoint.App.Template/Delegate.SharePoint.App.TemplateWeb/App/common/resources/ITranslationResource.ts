module $rootnamespace$ {
    // Used to ensure that our resources are always up to date (:
    export interface ITranslationResource extends ng.translate.ITranslationTable {
        HELLO: string
    }
}