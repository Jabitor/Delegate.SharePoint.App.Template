module $rootnamespace$ {
    export interface IAppService extends IAppServiceProvider, ng.IServiceProvider {
        // Ensures the provider related functions and ServiceProvider interface are implemented
    }
}