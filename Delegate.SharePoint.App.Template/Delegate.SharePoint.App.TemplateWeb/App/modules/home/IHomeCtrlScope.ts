module $rootnamespace$.Home {
    // Defines the interface for our Home Controller scope
    // Note that we're extending the angularjs scope
    export interface IHomeCtrlScope extends ng.IScope {

    }
}